### 异常处理
urllib.error 模块提供了 request 模块产生的异常处理方法，它主要包含了 URLError 和 HTTPError 两个类。
* URLError 类是 HTTPError 类的基类，可以捕获 request 模块产生的异常，它有一个 reason 属性（返回异常的原因）。
* HTTPError 类是 URLError 类的子类，专门处理 HTTP 请求的异常，它具有 3 个属性，分别为 reason（返回异常的原因），code（返回 HTTP 状态码），headers（返回请求头）。